import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#4255ff",
    },
    purple: {
      main: "#4255ff",
    },
    white: {
      main: "#ffffff",
      dark: "#4255ff",
    },
  },
});

export default theme;
