services:
  backend:
    build:
      context: .
      dockerfile: dockerfile
    container_name: flashlearn_backend
    env_file:
      - .env.docker
    ports:
      - "8005:8005"

  worker:
    build:
      context: .
      dockerfile: dockerfile
    container_name: flashlearn_worker
    command: uv run python manage.py start_worker
    env_file:
      - .env.docker
    depends_on:
      - backend

  frontend:
    build: ./frontend
    container_name: flashlearn_frontend
    ports:
      - "3001:80"
    depends_on:
      - backend
